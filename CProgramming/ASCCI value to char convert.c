// write a program that takes an ASCII  value from user and convert it to character.

#include<stdio.h>
int main()

{
    int i;
    printf("Please enter an integer: ");
    scanf("%d", &i);
    printf("The character of that value is %c",i);

    return 0;
}
