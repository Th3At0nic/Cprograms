/*
write a program that takes decimal number as input and convert it to octal number.
*/
#include<stdio.h>
int main()

{
    int num;
    printf("Enter a decimal number: ");
    scanf("%d",&num);
    printf("Octal number is: %o",num);

    return 0;
}
